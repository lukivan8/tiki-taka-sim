# Aggressive xG Wave G4

G3's paired G4-defense screen completed 36 passes with no harmful fallback and
far less clustering than the Strict-derived attempts, but generated zero dribbles
and zero shots. G4 targets that exact safe-pass loop.

When a non-goalkeeper owner below x=38 has no open pass progressing at least five
metres, the command mask supplies only a semantically validated forward/diagonal
carry. Open progressive passes and the inherited close-finish guard remain intact.
